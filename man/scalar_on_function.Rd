% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scalar_on_function.R
\name{scalar_on_function}
\alias{scalar_on_function}
\title{Scalar-on-Function Regression (Stage 2)}
\usage{
scalar_on_function(
  primary_trait,
  functional_profiles,
  basis = NULL,
  genomic_matrix = NULL,
  pedigree_matrix = NULL,
  environment_col = NULL,
  engine = "auto",
  ...
)
}
\arguments{
\item{primary_trait}{One of:
\itemize{
\item A named numeric vector with names corresponding to variety identifiers.
\item An \code{fda_data} object containing a \code{primary_trait} column (extracted
automatically).
\item A data.frame / data.table with columns \code{variety} and \code{yield} (or
the name specified via the first column).
}}

\item{functional_profiles}{One of:
\itemize{
\item An \code{fda_model} object from \code{\link[=fit_functional_profiles]{fit_functional_profiles()}} (preferred).
\item A numeric matrix of B-spline coefficients (varieties x basis functions),
in which case \code{basis} must also be supplied.
}}

\item{basis}{An \code{fda_basis} object (from \code{\link[=bspline_basis]{bspline_basis()}}). Required when
\code{functional_profiles} is a raw matrix; ignored when it is an \code{fda_model}
(the basis is extracted from the model object).}

\item{genomic_matrix}{Optional numeric matrix (symmetric, PSD) of genomic
relationships among varieties. Row/column names must match variety
identifiers. Used as \code{vm(variety, source = G)} in the mixed model.}

\item{pedigree_matrix}{Optional numeric matrix (symmetric, PSD) of
pedigree-based relationships. Same naming convention as \code{genomic_matrix}.}

\item{environment_col}{Character or \code{NULL}; name of the environment column
in \code{primary_trait} if multi-environment data is supplied. Default \code{NULL}
(single-environment analysis).}

\item{engine}{Character; estimation engine. One of \code{"auto"} (default),
\code{"asreml"}, or \code{"bayesreml"}. See \code{\link[=funcrop_engines]{funcrop_engines()}}.}

\item{...}{Additional arguments passed to the backend fitting function.}
}
\value{
An \code{fda_model} object containing:
\describe{
\item{fitted_curves}{Empty data.table (not applicable for Stage 2).}
\item{coefficient_function}{List with \code{time}, \code{beta}, \code{se}, \code{ci_lower},
\code{ci_upper} -- the estimated coefficient function \eqn{\hat{\beta}(t)}
evaluated on a fine grid.}
\item{variance_components}{data.table of estimated variance components.}
\item{predictions}{data.table of predicted primary trait values per
variety.}
\item{residuals}{Model residuals.}
\item{basis}{The \code{fda_basis} object used.}
\item{data}{The input \code{primary_trait} data.}
\item{engine}{Character string identifying the engine used.}
\item{call}{The matched function call.}
\item{extras}{List with \code{beta_coefs} (estimated B-spline coefficients for
\eqn{\beta(t)}), \code{J_matrix} (inner product matrix of B-spline basis),
\code{C_matrix} (functional covariate matrix), \code{raw_model}.}
}
}
\description{
Fits a scalar-on-function regression model that relates a scalar primary
trait (e.g., yield) to the variety-specific functional profiles estimated
in Stage 1 (via \code{\link[=fit_functional_profiles]{fit_functional_profiles()}}). The key output is the
coefficient function \eqn{\beta(t)}, which identifies the time periods
during which the secondary trait most strongly predicts the primary trait.
}
\details{
\subsection{Model formulation}{

\deqn{y_i = \alpha + \int \beta(t) \hat{f}_i(t) \, dt + u_i + \varepsilon_i}

where:
\itemize{
\item \eqn{\hat{f}_i(t)} is the Stage 1 estimated functional profile for
variety \eqn{i}.
\item \eqn{\beta(t) = \sum_k b_k B_k(t)} is the coefficient function,
expanded in the same B-spline basis.
\item The integral is approximated using the inner product matrix:
\deqn{J_{jk} = \int B_j(t) B_k(t) \, dt}
computed via numerical quadrature (composite Simpson's rule).
\item The functional covariate matrix is:
\deqn{C = \hat{A} J}
where \eqn{\hat{A}} is the variety x basis coefficient matrix from
Stage 1. Each row of \eqn{C} is the functional covariate for one
variety.
\item \eqn{u_i} is an optional genomic/pedigree random effect:
\eqn{u \sim N(0, \sigma_g^2 G)} where \eqn{G} is the relationship
matrix.
\item \eqn{\varepsilon_i \sim N(0, \sigma^2)} is the residual.
}
}

\subsection{Smoothness of beta(t)}{

The coefficient function is regularised via a P-spline penalty on the
B-spline coefficients \eqn{b}: the penalty matrix from the basis is
incorporated as a random effect precision structure (mixed model
representation of P-splines).
}
}
\examples{
\dontrun{
# Assuming `profiles` is an fda_model from fit_functional_profiles()
data(sim_grain_fill)

# Named yield vector
yield_vec <- setNames(
  sim_grain_fill[, .(yield = mean(yield)), by = variety]$yield,
  sim_grain_fill[, .(yield = mean(yield)), by = variety]$variety
)

# Stage 2: scalar-on-function regression
sof_fit <- scalar_on_function(
  primary_trait        = yield_vec,
  functional_profiles  = profiles,
  engine               = "auto"
)

# Inspect the coefficient function
plot(sof_fit, which = "coef")
coef(sof_fit)
}

}
\references{
Ramsay, J.O. and Silverman, B.W. (2005). \emph{Functional Data Analysis}
(2nd ed.). Springer.

Reiss, P.T. and Ogden, R.T. (2007). Functional principal component
regression and functional partial least squares. \emph{Journal of the American
Statistical Association}, 102(479), 984--996.

Marx, B.D. and Eilers, P.H.C. (1999). Generalized linear regression on
sampled signals and curves: a P-spline approach. \emph{Technometrics}, 41(1),
1--13.
}
\seealso{
\code{\link[=fit_functional_profiles]{fit_functional_profiles()}} for Stage 1, \code{\link[=compare_methods]{compare_methods()}} for
model comparison, \code{\link[=bspline_basis]{bspline_basis()}} for basis construction.
}
