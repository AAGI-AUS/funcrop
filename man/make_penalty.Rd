% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/penalty.R
\name{make_penalty}
\alias{make_penalty}
\title{Construct a Difference Penalty Matrix}
\usage{
make_penalty(n_basis, order = 2L)
}
\arguments{
\item{n_basis}{Integer; number of basis functions (columns of the B-spline
basis matrix). Must be >= 2.}

\item{order}{Integer; order of the difference penalty. Default is 2
(second-order, penalising curvature). \code{order = 1} corresponds to a random
walk penalty. Must satisfy \verb{1 <= order < n_basis}.}
}
\value{
A sparse penalty matrix of class \code{dgCMatrix} (from the Matrix
package), dimension \verb{n_basis x n_basis}. The matrix is symmetric and
positive semi-definite with rank \code{n_basis - order}.
}
\description{
Builds the penalty matrix \eqn{P = D_d^T D_d} where \eqn{D_d} is the
d-th order difference matrix of dimension \verb{(n_basis - d) x n_basis}. This
is the standard discrete penalty used in P-spline smoothing (Eilers & Marx,
1996).
}
\details{
For \code{order = 1}, the difference matrix \eqn{D_1} has entries
\eqn{(D_1)_{ij} = 1} if \eqn{j = i+1} and \eqn{-1} if \eqn{j = i}.
Higher-order difference matrices are obtained by recursive application:
\eqn{D_d = D_1^{(n-d+1)} D_{d-1}}, where the superscript denotes the
dimension of the first-order matrix.
}
\examples{
# Second-order penalty for 12 basis functions
P <- make_penalty(12, order = 2)
dim(P)
Matrix::rankMatrix(P)

# First-order penalty (random walk)
P1 <- make_penalty(12, order = 1)

}
\references{
Eilers, P.H.C. and Marx, B.D. (1996). Flexible smoothing with B-splines
and penalties. \emph{Statistical Science}, 11(2), 89--121.
}
\seealso{
\code{\link[=bspline_basis]{bspline_basis()}} for basis construction,
\code{\link[=make_Zspline]{make_Zspline()}} for mixed model reparameterisation.
}
