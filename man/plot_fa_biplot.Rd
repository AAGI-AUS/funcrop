% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_fa_biplot}
\alias{plot_fa_biplot}
\title{Factor-Analytic Biplot for GxE Interaction}
\usage{
plot_fa_biplot(
  model,
  factors = c(1L, 2L),
  point_size = 3,
  text_size = 3.5,
  ...
)
}
\arguments{
\item{model}{An \code{fda_model} object containing FA structure in extras
(e.g., \code{model$extras$fa_loadings} and \code{model$extras$fa_scores}), or
with variance components that can be decomposed.}

\item{factors}{Integer vector of length 2 specifying which FA factors to
plot. Default \code{c(1, 2)}.}

\item{point_size}{Numeric; size of points. Default 3.}

\item{text_size}{Numeric; size of labels. Default 3.5.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{ggplot} object (invisibly), or \code{NULL} for base R fallback.
}
\description{
Produces a biplot from the factor-analytic (FA) decomposition of
genotype-by-environment interaction effects estimated from B-spline
coefficients. Environment loadings and variety scores are overlaid in a
two-dimensional space showing the dominant patterns of GxE interaction.
}
\details{
The biplot displays:
\itemize{
\item \strong{Environment loadings} as points/arrows, showing how environments
differentiate varieties.
\item \strong{Variety scores} as labelled points, showing relative variety performance
across the FA factors.
}

If the \code{ggrepel} package is available, labels are automatically positioned
to avoid overlapping. Otherwise, \code{\link[ggplot2:geom_text]{ggplot2::geom_text()}} is used.
}
\examples{
\dontrun{
met_model <- fit_functional_profiles(sim_met_fda, ...)
plot_fa_biplot(met_model)
}

}
