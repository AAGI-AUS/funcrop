% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genomic.R
\name{make_pedigree_matrix}
\alias{make_pedigree_matrix}
\title{Construct a Pedigree-Based Relationship Matrix (A Matrix)}
\usage{
make_pedigree_matrix(pedigree)
}
\arguments{
\item{pedigree}{A data.frame (or data.table) with exactly three columns
in order: \code{id}, \code{sire}, \code{dam}. Unknown parents should be coded as \code{NA},
\code{"0"}, or \code{0}. The pedigree must be sorted such that parents appear
before their offspring (chronological order).}
}
\value{
A symmetric positive definite matrix of dimension n x n, where
n is the number of individuals. Row and column names are set to the
individual IDs.
}
\description{
Computes the numerator relationship matrix (A) from a pedigree data.frame
using Henderson's tabular method. The A matrix encodes the expected
proportion of alleles shared identical by descent between all pairs of
individuals.
}
\details{
Implements Henderson's (1976) tabular method without external dependencies.
Time complexity is O(n^2) in the number of individuals, which is adequate
for typical crop trial pedigrees (up to ~5000 individuals). For larger
pedigrees, consider using the \code{pedigreemm} or \code{nadiv} packages.

Inbreeding coefficients are computed on the diagonal: \eqn{A_{ii} = 1 + F_i},
where \eqn{F_i} is the inbreeding coefficient of individual \eqn{i}.
}
\examples{
ped <- data.frame(
  id   = c("A", "B", "C", "D", "E"),
  sire = c(NA,  NA,  "A", "A", "C"),
  dam  = c(NA,  NA,  "B", "B", "D")
)
A <- make_pedigree_matrix(ped)
print(round(A, 3))

}
\references{
Henderson, C.R. (1976). A simple method for computing the inverse of a
numerator relationship matrix used in prediction of breeding values.
\emph{Biometrics}, 32(1), 69--83. \doi{10.2307/2529339}
}
\seealso{
\code{\link[=make_genomic_matrix]{make_genomic_matrix()}}, \code{\link[=make_H_matrix]{make_H_matrix()}},
\code{\link[=check_relationship_matrix]{check_relationship_matrix()}}
}
