% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basis.R
\name{bspline_basis}
\alias{bspline_basis}
\title{Construct a B-Spline Basis with Difference Penalty}
\usage{
bspline_basis(
  x,
  n_knots = 10L,
  degree = 3L,
  knot_type = c("equally_spaced", "quantile", "custom"),
  knots = NULL,
  boundary = NULL,
  penalty_order = 2L
)
}
\arguments{
\item{x}{Numeric vector of evaluation points (e.g., time, wavelength, depth).
Must contain at least 2 unique values and no \code{NA} or non-finite values.}

\item{n_knots}{Integer; number of internal knots. Default is 10. Must be
at least 1. The total number of basis functions is \code{n_knots + degree + 1}.}

\item{degree}{Integer; polynomial degree of the B-spline. Default is 3
(cubic). Must be >= 0.}

\item{knot_type}{Character; method for placing internal knots. One of
\code{"equally_spaced"} (default), \code{"quantile"}, or \code{"custom"}. If \code{"custom"},
the \code{knots} argument must be supplied.}

\item{knots}{Numeric vector of internal knots. Required when
\code{knot_type = "custom"}, ignored otherwise. Must lie strictly within the
boundary knots.}

\item{boundary}{Numeric vector of length 2 giving the boundary knots
(lower, upper). Defaults to \code{range(x)} extended by 1\% on each side.}

\item{penalty_order}{Integer; order of the difference penalty matrix.
Default is 2 (second-order differences, penalising curvature). Must be
at least 1 and less than \code{n_basis}.}
}
\value{
An S3 object of class \code{"fda_basis"} (a list) containing:
\describe{
\item{B}{Basis matrix (n x p), where n = \code{length(x)} and
p = \code{n_basis} = \code{n_knots + degree + 1}.}
\item{knots}{Numeric vector of internal knots used.}
\item{boundary}{Numeric vector of length 2; boundary knots.}
\item{degree}{Integer; polynomial degree.}
\item{penalty_order}{Integer; order of the difference penalty.}
\item{P}{Penalty matrix (p x p); sparse \code{Matrix} of class \code{dgCMatrix}.
Constructed as D'D where D is the difference matrix of the specified
order.}
\item{x}{Numeric vector; the original evaluation points.}
\item{n_basis}{Integer; total number of basis functions.}
\item{knot_type}{Character; knot placement method used.}
}
}
\description{
Creates a B-spline basis matrix evaluated at a set of points, together with
a difference penalty matrix suitable for P-spline smoothing. This is the
core building block for all functional data representations in \code{funcrop}.
}
\details{
The basis matrix is computed using \code{\link[splines:splineDesign]{splines::splineDesign()}}, which handles
boundary evaluation correctly. Knots are augmented with repeated boundary
knots as required by the B-spline recursion.

The penalty matrix \eqn{P = D_d^T D_d} encodes a discrete approximation to
the d-th derivative penalty, where \eqn{D_d} is the d-th order difference
matrix operating on the B-spline coefficients (Eilers & Marx, 1996).
}
\examples{
# Basic cubic B-spline basis with 10 internal knots
x <- seq(0, 1, length.out = 100)
basis <- bspline_basis(x, n_knots = 10, degree = 3)
print(basis)

# Quantile-based knot placement (better for skewed data)
set.seed(42)
x_skew <- sort(rbeta(200, 2, 5))
basis_q <- bspline_basis(x_skew, n_knots = 8, knot_type = "quantile")

\donttest{
# Visualise basis functions
plot(basis)
}

}
\references{
Eilers, P.H.C. and Marx, B.D. (1996). Flexible smoothing with B-splines
and penalties. \emph{Statistical Science}, 11(2), 89--121.
}
\seealso{
\code{\link[=make_penalty]{make_penalty()}} for standalone penalty construction,
\code{\link[=make_Zspline]{make_Zspline()}} for mixed model reparameterisation,
\code{\link[=tensor_bspline_basis]{tensor_bspline_basis()}} for 2D tensor product bases.
}
