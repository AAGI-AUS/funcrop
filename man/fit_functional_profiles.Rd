% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_profiles.R
\name{fit_functional_profiles}
\alias{fit_functional_profiles}
\title{Fit Variety-Specific Functional Profiles (Stage 1)}
\usage{
fit_functional_profiles(
  data,
  time_col = "time",
  value_col = NULL,
  id_col = "id",
  group_col = "variety",
  spatial_row_col = NULL,
  spatial_col_col = NULL,
  basis = NULL,
  n_knots = 10L,
  degree = 3L,
  penalty_order = 2L,
  spatial = c("none", "ar1", "ar1ar1", "spline"),
  engine = "auto",
  ...
)
}
\arguments{
\item{data}{An \code{fda_data} object (from \code{\link[=fda_data]{fda_data()}}) or a data.frame /
data.table containing the observations. If a raw data.frame, the column
mapping arguments below must be specified.}

\item{time_col}{Character; name of the time column in \code{data}.
Default \code{"time"}. Ignored when \code{data} is an \code{fda_data} object.}

\item{value_col}{Character or \code{NULL}; name of the response (value) column.
Default \code{NULL}, which uses \code{"value"} for \code{fda_data} or requires explicit
specification for raw data.}

\item{id_col}{Character; name of the observational unit identifier column
(e.g., plot). Default \code{"id"}.}

\item{group_col}{Character; name of the variety/genotype grouping column.
Default \code{"variety"}.}

\item{spatial_row_col}{Character or \code{NULL}; name of the spatial row column.
Default \code{NULL} (no spatial modelling unless detected from \code{fda_data}).}

\item{spatial_col_col}{Character or \code{NULL}; name of the spatial column column.
Default \code{NULL}.}

\item{basis}{An \code{fda_basis} object (from \code{\link[=bspline_basis]{bspline_basis()}}). If \code{NULL}
(default), a basis is constructed automatically from the unique time points
in \code{data} using \code{n_knots}, \code{degree}, and \code{penalty_order}.}

\item{n_knots}{Integer; number of internal knots for automatic basis
construction. Default 10. Ignored if \code{basis} is supplied.}

\item{degree}{Integer; B-spline polynomial degree. Default 3 (cubic).
Ignored if \code{basis} is supplied.}

\item{penalty_order}{Integer; order of the difference penalty for the
P-spline. Default 2 (penalises curvature). Ignored if \code{basis} is supplied.}

\item{spatial}{Character; type of spatial error model. One of \code{"none"}
(default), \code{"ar1"} (first-order autoregressive in rows), \code{"ar1ar1"}
(separable AR1 x AR1 in rows and columns), or \code{"spline"} (2D spatial
spline). Requires \code{spatial_row_col} and \code{spatial_col_col} to be specified
(except \code{"none"}).}

\item{engine}{Character; estimation engine. One of \code{"auto"} (default),
\code{"asreml"}, or \code{"bayesreml"}. See \code{\link[=funcrop_engines]{funcrop_engines()}}.}

\item{...}{Additional arguments passed to the backend fitting function
(e.g., \code{control} for ASReml, \code{mcmc_control} for bayesreml).}
}
\value{
An \code{fda_model} object (see \code{new_fda_model()}) containing:
\describe{
\item{fitted_curves}{data.table of variety-specific fitted curves on a
fine time grid, with columns \code{id}, \code{time}, \code{fitted}, \code{se}.}
\item{coefficient_function}{Empty list (not applicable for Stage 1; the
coefficient function beta(t) is estimated in Stage 2).}
\item{variance_components}{data.table of estimated variance components.}
\item{predictions}{data.table of variety-level predicted values.}
\item{residuals}{Model residuals.}
\item{basis}{The \code{fda_basis} object used.}
\item{data}{The input data (as \code{fda_data}).}
\item{engine}{Character string identifying the engine used.}
\item{call}{The matched function call.}
\item{extras}{List with \code{spline_blups} (variety x basis coefficient matrix),
\code{spline_decomposition} (from \code{\link[=make_Zspline]{make_Zspline()}}), \code{raw_model} (backend
model object), and \code{convergence} diagnostics.}
}
}
\description{
Fits a spatial + temporal linear mixed model per trial to extract
variety-specific functional profiles (smooth curves) from repeated
measurements of a secondary trait. This is the first stage of the two-stage
FDA approach: Stage 1 recovers the shape of each variety's functional
response; Stage 2 (via \code{\link[=scalar_on_function]{scalar_on_function()}}) regresses a primary trait
(e.g., yield) onto these profiles.
}
\details{
\subsection{Model formulation}{

For each trial / environment, the model is:
\deqn{y_{ijk}(t) = f_v(t) + b_j + s(r, c) + \varepsilon_{ijk}(t)}

where:
\itemize{
\item \eqn{f_v(t) = \sum_k \alpha_{vk} B_k(t)} is the variety-specific curve,
with \eqn{\alpha_v \sim N(0, \sigma_v^2 I)} as random B-spline
coefficients (P-spline mixed model representation).
\item \eqn{b_j} is a block effect (if \code{block} column is present).
\item \eqn{s(r, c)} is an optional spatial error model (AR1, AR1xAR1, or
spline).
\item \eqn{\varepsilon_{ijk}(t) \sim N(0, \sigma^2)} is the residual.
}

The variety-specific B-spline random effects are structured as a Kronecker
product: \eqn{Z = I_V \otimes Z_{spline}}, where \eqn{Z_{spline}} comes
from \code{\link[=make_Zspline]{make_Zspline()}} and \eqn{I_V} is the identity over varieties. This
yields \eqn{V \times K} random effect levels (V varieties, K spline
coefficients).
}

\subsection{Backend dispatch}{

For ASReml: uses \code{str()} specification from \code{.asreml_build_spline_str()}
with penalised variance structure. For bayesreml: uses known matrices from
\code{.bayesreml_build_spline_str()} with penalty-as-precision prior.
}
}
\examples{
\dontrun{
# Load simulated grain-fill data
data(sim_grain_fill)

# Create fda_data object
fd <- fda_data(
  time  = sim_grain_fill$time,
  value = sim_grain_fill$grain_weight,
  id    = sim_grain_fill$plot_id,
  group = sim_grain_fill$variety,
  spatial_row = sim_grain_fill$row,
  spatial_col = sim_grain_fill$col,
  primary_trait = sim_grain_fill$yield,
  primary_trait_name = "yield_t_ha"
)

# Fit functional profiles (Stage 1)
profiles <- fit_functional_profiles(
  data    = fd,
  n_knots = 6,
  spatial = "ar1ar1",
  engine  = "auto"
)

print(profiles)
plot(profiles, which = "fitted")

# Extract BLUPs for Stage 2
blup_matrix <- profiles$extras$spline_blups
}

}
\references{
Verbyla, A.P., Cavanagh, C.R. and Verbyla, K.L. (2012). Whole-genome
analysis of multienvironment or multitrait QTL in MAGIC. \emph{G3: Genes,
Genomes, Genetics}, 2(9), 1085--1093.

De Faveri, J., Verbyla, A.P., Pitchford, W.S., Venkatanagappa, S. and
Cullis, B.R. (2015). Statistical methods for analysis of multi-harvest data
from perennial pasture variety selection trials. \emph{Crop and Pasture Science},
66(9), 947--962.
}
\seealso{
\code{\link[=scalar_on_function]{scalar_on_function()}} for Stage 2, \code{\link[=bspline_basis]{bspline_basis()}} for basis
construction, \code{\link[=make_Zspline]{make_Zspline()}} for mixed model reparameterisation.
}
