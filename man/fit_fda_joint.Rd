% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_joint.R
\name{fit_fda_joint}
\alias{fit_fda_joint}
\title{Fit a Joint Model for Primary and Functional Secondary Traits}
\usage{
fit_fda_joint(
  data,
  primary_col = "yield",
  time_col = "time",
  secondary_col = NULL,
  id_col = "id",
  group_col = "variety",
  spatial_row_col = NULL,
  spatial_col_col = NULL,
  basis = NULL,
  n_knots = 10L,
  degree = 3L,
  penalty_order = 2L,
  spatial = c("none", "ar1", "ar1ar1"),
  genomic_matrix = NULL,
  pedigree_matrix = NULL,
  engine = "auto",
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or \link[data.table:data.table]{data.table::data.table} in long format
containing both the functional secondary trait observations (one row per
id x time combination) and the primary trait (constant within each id).
May also be an \code{fda_data} object.}

\item{primary_col}{Character; column name for the scalar primary trait
(e.g., yield). Default \code{"yield"}.}

\item{time_col}{Character; column name for the time/index variable of the
functional trait. Default \code{"time"}.}

\item{secondary_col}{Character or \code{NULL}; column name for the functional
secondary trait values. If \code{NULL} (default), inferred as the first numeric
column that is not \code{primary_col}, \code{time_col}, or a spatial column.}

\item{id_col}{Character; column name identifying observational units
(e.g., plot). Default \code{"id"}.}

\item{group_col}{Character; column name for the grouping factor
(e.g., variety, genotype). Default \code{"variety"}.}

\item{spatial_row_col}{Character or \code{NULL}; column name for spatial row
coordinates. Default \code{NULL} (no spatial modelling).}

\item{spatial_col_col}{Character or \code{NULL}; column name for spatial column
coordinates. Default \code{NULL}.}

\item{basis}{An \code{fda_basis} object (from \code{\link[=bspline_basis]{bspline_basis()}}). If \code{NULL}
(default), a basis is constructed from the data using \code{n_knots}, \code{degree},
and \code{penalty_order}.}

\item{n_knots}{Integer; number of internal B-spline knots if \code{basis} is
\code{NULL}. Default 10.}

\item{degree}{Integer; B-spline polynomial degree. Default 3 (cubic).}

\item{penalty_order}{Integer; order of the difference penalty. Default 2.}

\item{spatial}{Character; spatial correlation structure for residuals. One
of \code{"none"} (default), \code{"ar1"} (AR1 in rows), or \code{"ar1ar1"} (separable
AR1 x AR1 in rows and columns).}

\item{genomic_matrix}{Optional numeric matrix (square, symmetric) of genomic
relationships among groups. Row/column names must match levels of
\code{group_col}. Default \code{NULL}.}

\item{pedigree_matrix}{Optional numeric matrix of pedigree-based
relationships. Same naming requirements as \code{genomic_matrix}.
Default \code{NULL}.}

\item{engine}{Character; estimation engine. One of \code{"auto"} (default),
\code{"asreml"}, or \code{"bayesreml"}. See \code{\link[=funcrop_engines]{funcrop_engines()}}.}

\item{...}{Additional arguments passed to the backend fitting function
(e.g., \code{maxiter}, \code{workspace} for ASReml; \code{mcmc_control} for bayesreml).}
}
\value{
An \code{fda_model} object containing:
\describe{
\item{fitted_curves}{\link[data.table:data.table]{data.table::data.table} of variety-specific fitted
functional curves for the secondary trait.}
\item{coefficient_function}{List with the implied relationship
\eqn{\beta(t)} between the secondary functional trait and the primary
scalar trait.}
\item{variance_components}{\link[data.table:data.table]{data.table::data.table} of estimated variance
components including the cross-trait correlations.}
\item{predictions}{\link[data.table:data.table]{data.table::data.table} of predicted primary trait
values per group.}
\item{basis}{The \code{fda_basis} object used for the functional trait.}
\item{engine}{Character string identifying the backend used.}
}
}
\description{
Fits a single-stage model that simultaneously models a scalar primary trait
(e.g., yield) and a functional secondary trait (e.g., grain-fill over time),
allowing the functional trait to inform estimation of the primary trait.
This avoids the error propagation issues inherent in the two-stage approach
where functional profiles are first estimated, then used as predictors.
}
\section{Statistical Model}{

The joint model treats the secondary trait at each time point and the primary
trait as a multivariate response. In stacked form:

\deqn{
\begin{bmatrix} y_{\text{secondary}}(t) \\ y_{\text{primary}} \end{bmatrix}
= \begin{bmatrix} f_v(t) + \text{spatial} + \varepsilon_s(t) \\
  \alpha + \int \beta(t) f_v(t)\, dt + \varepsilon_p \end{bmatrix}
}

where \eqn{f_v(t) = \sum_k \alpha_{vk} B_k(t)} is the variety-specific
B-spline random effect for the secondary trait, and the primary trait is
linked through the functional integral \eqn{\int \beta(t) f_v(t)\, dt}.

The model is fitted as a bivariate mixed model with:
\itemize{
\item A trait indicator variable distinguishing secondary vs primary observations
\item B-spline basis x variety interactions for the secondary trait
\item Numerical integration weights for the functional covariate in the primary
trait equation
\item Correlated random effects across traits via \code{us(trait)} structures
}
}

\section{Implementation}{

Data are stacked with a \code{trait_type} factor (\code{"secondary"}, \code{"primary"}).
For secondary-trait rows, the design matrix uses B-spline basis x variety
interactions. For primary-trait rows, the design matrix uses the numerical
integral of the B-spline basis functions (trapezoidal rule), creating a
functional covariate. Variety-level random effects are correlated across
both trait types.
}

\examples{
\dontrun{
data(sim_grain_fill)

# Fit the joint model
joint_fit <- fit_fda_joint(
  data          = sim_grain_fill,
  primary_col   = "yield",
  time_col      = "time",
  secondary_col = "grain_weight",
  id_col        = "plot_id",
  group_col     = "variety",
  spatial_row_col = "row",
  spatial_col_col = "col",
  n_knots       = 6,
  spatial       = "ar1ar1",
  engine        = "asreml"
)
print(joint_fit)
summary(joint_fit)
}

}
\references{
De Boor, C. (2001). \emph{A Practical Guide to Splines} (Revised ed.).
Springer.

Eilers, P.H.C. and Marx, B.D. (1996). Flexible smoothing with B-splines
and penalties. \emph{Statistical Science}, 11(2), 89--121.
}
\seealso{
\code{\link[=fit_functional_profiles]{fit_functional_profiles()}} for Stage 1 of the two-stage approach,
\code{\link[=scalar_on_function]{scalar_on_function()}} for Stage 2, \code{\link[=bspline_basis]{bspline_basis()}} for basis
construction.
}
