% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R
\name{fda_data}
\alias{fda_data}
\title{Create a functional data object}
\usage{
fda_data(
  time,
  value,
  id,
  group = NULL,
  spatial_row = NULL,
  spatial_col = NULL,
  primary_trait = NULL,
  primary_trait_name = NULL,
  ...
)
}
\arguments{
\item{time}{Numeric vector of time points (or other continuous dimension
such as thermal time, depth, wavelength).}

\item{value}{Numeric vector of measured values (the secondary / functional
trait, e.g., NDVI).}

\item{id}{Character or factor identifying the observational unit (e.g.,
plot, plant).}

\item{group}{Optional character or factor for grouping (e.g., variety,
genotype). Default \code{NULL}.}

\item{spatial_row}{Optional integer vector of spatial row positions. Default
\code{NULL}.}

\item{spatial_col}{Optional integer vector of spatial column positions.
Default \code{NULL}.}

\item{primary_trait}{Optional numeric vector of primary trait values (e.g.,
yield). Must contain exactly one value per unique \code{id}. Recycled
internally to match the observation-level data. Default \code{NULL}.}

\item{primary_trait_name}{Optional character string naming the primary trait
(e.g., \code{"yield_t_ha"}). Default \code{NULL}.}

\item{...}{Additional named vectors of the same length as \code{time}, stored as
extra columns in the underlying data.table.}
}
\value{
An object of class \code{fda_data}, inheriting from \code{data.table} and
\code{data.frame}. Contains an attribute \code{"fda_meta"} with metadata (trait
names, id column, etc.).
}
\description{
Constructs a validated \code{fda_data} object wrapping functional observations
(e.g., repeated measurements of a secondary trait over time) into a
structured \link[data.table:data.table]{data.table::data.table} with metadata. Designed for crop variety
trial data where secondary functional traits (NDVI, canopy temperature,
grain-fill rate) are related to a primary trait (e.g., yield).
}
\section{Validation}{

\itemize{
\item \code{time}, \code{value}, and \code{id} must be non-NA and of equal length.
\item \code{time} and \code{value} must be numeric and finite.
\item If supplied, \code{primary_trait} must have exactly one value per unique \code{id}.
\item Spatial columns must be integer-valued.
}
}

\examples{
# Simulate simple functional data
set.seed(42)
ids <- rep(paste0("plot_", 1:5), each = 10)
times <- rep(seq(100, 190, by = 10), times = 5)
vals <- sin(times / 50) + rnorm(50, sd = 0.1)
grps <- rep(c("var_A", "var_B", "var_A", "var_B", "var_A"), each = 10)
yields <- rep(c(3.2, 4.1, 3.8, 4.5, 3.0), each = 10)

fd <- fda_data(
  time = times,
  value = vals,
  id = ids,
  group = grps,
  primary_trait = yields,
  primary_trait_name = "yield_t_ha"
)
print(fd)
summary(fd)

}
