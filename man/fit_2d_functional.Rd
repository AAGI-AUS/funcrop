% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_joint.R
\name{fit_2d_functional}
\alias{fit_2d_functional}
\title{Fit 2D Functional Profiles via Tensor Product B-Splines}
\usage{
fit_2d_functional(
  data,
  time_col = "time",
  depth_col = "depth",
  value_col = "value",
  id_col = "id",
  group_col = "variety",
  n_knots1 = 10L,
  n_knots2 = 10L,
  degree1 = 3L,
  degree2 = 3L,
  spatial_row_col = NULL,
  spatial_col_col = NULL,
  engine = "auto",
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or \link[data.table:data.table]{data.table::data.table} in long format with
one row per observation (id x dim1 x dim2).}

\item{time_col}{Character; column name for the first functional dimension
(e.g., time). Default \code{"time"}.}

\item{depth_col}{Character; column name for the second functional dimension
(e.g., depth, wavelength). Default \code{"depth"}.}

\item{value_col}{Character; column name for the response. Default \code{"value"}.}

\item{id_col}{Character; column name for the observational unit. Default
\code{"id"}.}

\item{group_col}{Character; column name for the grouping factor
(e.g., variety). Default \code{"variety"}.}

\item{n_knots1}{Integer; number of internal knots for the first dimension.
Default 10.}

\item{n_knots2}{Integer; number of internal knots for the second dimension.
Default 10.}

\item{degree1}{Integer; B-spline degree for dimension 1. Default 3.}

\item{degree2}{Integer; B-spline degree for dimension 2. Default 3.}

\item{spatial_row_col}{Character or \code{NULL}; spatial row column. Default
\code{NULL}.}

\item{spatial_col_col}{Character or \code{NULL}; spatial column column. Default
\code{NULL}.}

\item{engine}{Character; estimation engine. Default \code{"auto"}.}

\item{...}{Additional arguments passed to the backend.}
}
\value{
An \code{fda_model} object with \code{model_type = "2d_functional"} in
extras. The \code{fitted_curves} component contains fitted 2D surfaces
per group, and \code{basis} contains the \code{fda_tensor_basis} object.
}
\description{
Models a functional trait observed over two continuous indices (e.g., time
and soil depth, time and wavelength) using tensor product B-splines with
separable penalties. Group-specific (e.g., variety-specific) 2D surfaces
are estimated as random effects.
}
\section{Statistical Model}{

\deqn{y(t, d) = f_v(t, d) + \text{spatial} + \varepsilon}
where \eqn{f_v(t, d) = \sum_{j} \sum_{k} \alpha_{vjk} B_{1j}(t) B_{2k}(d)}
is a variety-specific tensor product random surface, and the penalty is
the standard Kronecker sum:
\eqn{P = P_1 \otimes I_2 + I_1 \otimes P_2}.
}

\examples{
\dontrun{
# Simulated 2D functional data: time x depth
set.seed(123)
n_obs <- 500
dt2d <- data.table::data.table(
  id      = rep(paste0("P", 1:10), each = 50),
  variety = rep(rep(c("V1", "V2"), each = 5), each = 50),
  time    = runif(n_obs, 0, 100),
  depth   = runif(n_obs, 0, 1),
  value   = rnorm(n_obs)
)

fit2d <- fit_2d_functional(
  data      = dt2d,
  time_col  = "time",
  depth_col = "depth",
  value_col = "value",
  id_col    = "id",
  group_col = "variety",
  n_knots1  = 6, n_knots2 = 5
)
}

}
\references{
Marx, B.D. and Eilers, P.H.C. (2005). Multidimensional penalized signal
regression. \emph{Technometrics}, 47(1), 13--22.

Wood, S.N. (2006). Low-rank scale-invariant tensor product smooths for
generalized additive mixed models. \emph{Biometrics}, 62(4), 1025--1036.
}
\seealso{
\code{\link[=tensor_bspline_basis]{tensor_bspline_basis()}} for tensor product basis construction,
\code{\link[=scalar_on_2d_function]{scalar_on_2d_function()}} for relating 2D profiles to a scalar response.
}
