% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_backend_comparison}
\alias{plot_backend_comparison}
\title{Compare REML and Bayesian Model Fits}
\usage{
plot_backend_comparison(model_reml, model_bayes, varieties = NULL, ...)
}
\arguments{
\item{model_reml}{An \code{fda_model} object fitted with the ASReml backend
(\code{engine = "asreml"}).}

\item{model_bayes}{An \code{fda_model} object fitted with the bayesreml backend
(\code{engine = "bayesreml"}).}

\item{varieties}{Character vector of variety names to include in the
curves panel, or \code{NULL} (default) for all. Plots may be cluttered if
many varieties are included; consider limiting to 5--10.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{ggplot} object (invisibly), or \code{NULL} for base R fallback.
When ggplot2 is available, both panels are arranged side by side.
}
\description{
Produces a two-panel comparison of results from ASReml-R (REML) and
bayesreml (Bayesian) backends. The left panel compares fitted functional
curves; the right panel compares coefficient function estimates. REML
results are shown as point estimates with standard error bands, while
Bayesian results show the posterior mean with credible intervals.
}
\details{
Colour coding:
\itemize{
\item ASReml (REML): solid lines with dashed CI bands
\item bayesreml (Bayesian): semi-transparent ribbons for credible intervals
}
}
\examples{
\dontrun{
model_a <- fit_functional_profiles(..., engine = "asreml")
model_b <- fit_functional_profiles(..., engine = "bayesreml")
plot_backend_comparison(model_a, model_b)
}

}
