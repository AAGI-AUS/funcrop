% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genomic.R
\name{make_genomic_matrix}
\alias{make_genomic_matrix}
\title{Construct a Genomic Relationship Matrix}
\usage{
make_genomic_matrix(
  markers,
  method = c("vanraden1", "vanraden2"),
  min_maf = 0.01,
  scale = TRUE
)
}
\arguments{
\item{markers}{Numeric matrix of dimension n_varieties x n_markers.
Coded as 0, 1, 2 (minor allele counts) or -1, 0, 1 (centred). Row names
should identify varieties; column names (if present) identify markers.}

\item{method}{Character: \code{"vanraden1"} (default) or \code{"vanraden2"}.
\itemize{
\item \strong{VanRaden Method 1}: \eqn{G = ZZ' / (2 \sum p_j(1-p_j))}, where
\eqn{Z_{ij} = M_{ij} - 2p_j} and \eqn{p_j} is the allele frequency
at marker \eqn{j}.
\item \strong{VanRaden Method 2}: \eqn{G = ZDZ' / n_m}, where
\eqn{D = \mathrm{diag}(1/(2 p_j (1-p_j)))}.
}}

\item{min_maf}{Numeric: minimum minor allele frequency filter. Markers with
MAF below this threshold are removed. Default 0.01.}

\item{scale}{Logical: if \code{TRUE} (default), scale G so that the mean
diagonal equals 1.}
}
\value{
A symmetric positive (semi-)definite matrix of dimension
n_varieties x n_varieties, with row/colnames from \code{rownames(markers)}.
}
\description{
Computes the genomic relationship matrix (G) from marker data using
the VanRaden (2008) Method 1 or Method 2. This is a convenience function;
users may also supply pre-computed matrices to fitting functions directly.
}
\details{
Missing marker values (\code{NA}) are imputed to the column mean (i.e., twice
the allele frequency) prior to computation. If many markers are missing,
consider using a dedicated imputation tool first.
}
\examples{
set.seed(123)
# Simulate 20 varieties, 500 markers (coded 0/1/2)
M <- matrix(sample(0:2, 20 * 500, replace = TRUE,
                    prob = c(0.25, 0.50, 0.25)),
            nrow = 20, ncol = 500)
rownames(M) <- paste0("var_", seq_len(20))

G <- make_genomic_matrix(M, method = "vanraden1")
dim(G)
isSymmetric(G)

}
\references{
VanRaden, P.M. (2008). Efficient methods to compute genomic predictions.
\emph{Journal of Dairy Science}, 91(11), 4414--4423.
\doi{10.3168/jds.2007-0980}
}
\seealso{
\code{\link[=make_pedigree_matrix]{make_pedigree_matrix()}}, \code{\link[=make_H_matrix]{make_H_matrix()}},
\code{\link[=check_relationship_matrix]{check_relationship_matrix()}}
}
