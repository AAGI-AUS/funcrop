% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_joint.R
\name{scalar_on_2d_function}
\alias{scalar_on_2d_function}
\title{Scalar-on-2D-Function Regression}
\usage{
scalar_on_2d_function(
  primary_trait,
  functional_profiles_2d,
  basis = NULL,
  genomic_matrix = NULL,
  engine = "auto",
  ...
)
}
\arguments{
\item{primary_trait}{A named numeric vector of primary trait values (one per
group/variety) or a \link[data.table:data.table]{data.table::data.table} with columns \code{group} and
\code{value}.}

\item{functional_profiles_2d}{An \code{fda_model} object from
\code{\link[=fit_2d_functional]{fit_2d_functional()}}, or a list containing \code{fitted_surfaces} (a
\link[data.table:data.table]{data.table::data.table} with columns \code{group}, \code{time}, \code{depth}, \code{fitted})
and \code{basis} (an \code{fda_tensor_basis} object).}

\item{basis}{An \code{fda_tensor_basis} object. If \code{NULL}, extracted from
\code{functional_profiles_2d}.}

\item{genomic_matrix}{Optional genomic relationship matrix. Default \code{NULL}.}

\item{engine}{Character; estimation engine. Default \code{"auto"}.}

\item{...}{Additional arguments passed to the backend.}
}
\value{
An \code{fda_model} object where:
\describe{
\item{coefficient_function}{Contains the estimated 2D coefficient surface
\eqn{\beta(t, d)} as a data.table with columns \code{time}, \code{depth}, \code{beta},
\code{se}, \code{ci_lower}, \code{ci_upper}.}
\item{predictions}{Predicted primary trait values for each group.}
}
}
\description{
Extends scalar-on-function regression to functional predictors observed over
two continuous dimensions (e.g., time x depth). The model relates a scalar
primary trait to a 2D functional predictor via double integration:
}
\details{
\deqn{y_i = \alpha + \int_t \int_d \beta(t, d) f_i(t, d)\, dt\, dd
  + \varepsilon_i}

where \eqn{\beta(t, d)} is a 2D coefficient surface estimated via tensor
product B-splines with penalised regression, and \eqn{f_i(t, d)} are
the group-specific 2D functional profiles.
}
\examples{
\dontrun{
# After fitting 2D profiles:
# fit2d <- fit_2d_functional(...)
# primary_vals <- c(V1 = 4.2, V2 = 3.8, ...)
# sof2d <- scalar_on_2d_function(primary_vals, fit2d)
}

}
\references{
Marx, B.D. and Eilers, P.H.C. (2005). Multidimensional penalized signal
regression. \emph{Technometrics}, 47(1), 13--22.
}
\seealso{
\code{\link[=fit_2d_functional]{fit_2d_functional()}} for 2D profile estimation,
\code{\link[=scalar_on_function]{scalar_on_function()}} for the 1D equivalent.
}
